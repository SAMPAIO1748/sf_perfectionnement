{% extends "front/base.html.twig" %}

{% block main %}

	<h1>Les résultats de la recherche
		{{ term }}
		sont :</h1>

	{% for article in articles %}
		<h2>{{ article.title }}</h2>
	{% endfor %}

{% endblock %}
